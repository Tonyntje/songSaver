
const songList = (state = {}, action) => {
    switch(action.type) {
        case 'ADD_SONG':
            return state
        case 'DELETE_SONG':
            return state
        default:
            return state
    }
}

export default songList